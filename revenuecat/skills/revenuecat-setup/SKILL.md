---
name: revenuecat-setup
description: Install and configure the RevenueCat SDK in a new or existing app. Use when the user asks to add RevenueCat, install Purchases, configure the Purchases SDK, set up a RevenueCat API key, or initialize Purchases on iOS, Android, Kotlin Multiplatform, Flutter, or React Native.
---

# revenuecat-setup

The canonical instructions for this skill live in [`README.md`](README.md). Read `README.md` now. It contains the platform detection algorithm, shared concepts that apply across all platforms, and the pointer to `platforms/<detected>.md` for the implementation. Do not skip it.
